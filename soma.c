#include <stdio.h>
int principal (){
    int x, y, soma;
    printf("Digite o valor de x: ");
    scanf("%d", &x);
    printf("Digite o valor de y: ");
    sacnf("%d", &y);
    soma = x + y;
    printf("Soma = %d\n", soma);
    return 0;

}